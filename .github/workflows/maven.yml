name: Update Jira Fix Versions from Tag

on:
  push:
    tags:
      - 'v*'

jobs:
  update-jira:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get Jira Issue Keys from commit message and update Fix Versions
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        run: |
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          echo "🧷 Tag: $TAG_NAME"

          # 取得 tag 對應的 commit message
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "📄 Commit message: $COMMIT_MSG"

          # 擷取 Jira issue keys
          ISSUE_KEYS=$(echo "$COMMIT_MSG" | grep -oE '[A-Z]+-[0-9]+' | sort -u)

          if [ -z "$ISSUE_KEYS" ]; then
            echo "⚠️ No Jira issue keys found in commit message. Exiting."
            exit 0
          fi

          for ISSUE_KEY in $ISSUE_KEYS; do
            echo "🔧 Updating $ISSUE_KEY to fixVersion: $TAG_NAME"

            PROJECT_KEY="${ISSUE_
